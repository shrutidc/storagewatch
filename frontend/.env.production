# Used by `npm run build`. These three values are public by design — an SPA
# ships them to the browser, so they appear in any network trace. The client
# secret is NOT here and must never be: the browser never sees it.
VITE_AUTH0_DOMAIN=dev-q6slqq3gylewwmzs.us.auth0.com
VITE_AUTH0_CLIENT_ID=YRPc7XtVFPFeo46MTtY9dpITdhzsC3Hg
VITE_AUTH0_AUDIENCE=storagewatch-api
