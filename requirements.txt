# Everything needed to run the whole stack locally on the monitored Mac.
# Deployment installs backend/requirements.txt alone — see Dockerfile.
-r backend/requirements.txt
-r collector/requirements.txt
